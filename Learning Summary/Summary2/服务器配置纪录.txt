这星期我负责给服务器配置uwsgi+nginx，在起初遇到了很多bug无法解决，可能是服务器和教程中的情况不太一样，在后来配置过程中
服务器突然无法连接，静态ip直接ping不通，实在没有办法，只能将原来的服务器删除重新开了一个虚拟机，然后重新配置了python3
django和uwsgi和nginx，最终配置完成，但出现了127.0.0.1内端口无法连接的情况，nginx无法在8000端口运行，提示8000端口已被
占用，目前还未解决该问题，至于git，我发现git与服务器连接后有许多操作还要重新学习，耗费时间，且该程序代码量应该不至于需要
git操作，因此我没有配置。我选择安装了xftp来实现本地文件与服务器端文件的传输。